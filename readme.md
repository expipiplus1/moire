# A tool to calculate moire patterns.

- `l` is the length of the base windows
- `s` is the length of the painting segment

The output is a list of indices in the base windows where `s` has painted,
starting at 0
